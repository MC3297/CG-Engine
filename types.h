#pragma once

#include <vector>
#include <stack>
#include <cassert>
#include <string>
#include <cmath>
#include <iostream>
#include <algorithm>

using std::string;
using std::to_string;
using std::vector;
using std::cin;
using std::cout;
using std::swap;
using std::abs;
using std::min;
using std::max;
using std::isnan;

using db = long double;